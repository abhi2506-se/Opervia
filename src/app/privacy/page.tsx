export default function PrivacyPage() {
  return (
    <div className="min-h-screen px-6 py-10 max-w-2xl mx-auto text-sm leading-relaxed" style={{ color: "var(--ink)" }}>
      <h1 className="text-lg font-semibold mb-4" style={{ color: "var(--navy-deep)" }}>Privacy Policy</h1>
      <p className="mb-3" style={{ color: "var(--danger)" }}>
        Placeholder — this page has not been reviewed by a qualified legal professional. Replace this
        content with a counsel-reviewed Privacy Policy describing what client data is collected
        (profile details, project files, payment metadata), how it is stored, and how clients can
        request access or deletion, before accepting real client data.
      </p>
    </div>
  );
}
